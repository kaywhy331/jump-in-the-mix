# Product quality gates

Use [Product completion](PRODUCT_COMPLETION.md) for current working-tree evidence. The published test-release results below describe that older deployment, not the full current local implementation.

Current worker recovery checks include real competing processes, a hard crash between imported rows, connection-outage recovery and the normal five-minute preparation interval without web traffic. Import database tests cover atomic create/merge/replace receipts, cancellation and takeover races, closed-batch retry denial, safe HTTP failures and the populated upgrade. See [worker recovery qualification](WORKER_RECOVERY_QUALIFICATION.md) for repeatable commands and the required stop of old workers before its migration. Intended hosted supervision and unattended scheduling remain separate release gates.

The September 7 [production structure and launch plan](PRODUCTION_LAUNCH_PLAN.md) records the anticipated infrastructure and planned Stripe payments. Deployment remains deferred. The initial invitation launch is free. Payment qualification is required before any later decision to charge customers.

The continuing objective is an exceptional, intuitive follow-up product that achieves the [canonical product goals](CANONICAL_PRODUCT_DECISIONS.md), including the newer configurable journey, calendar, and multi-source intake requirements. Passing the checks already written is not sufficient to close this objective. An award-level claim remains unproven.

This table separates inspected evidence from requirements that still need qualification. Older reports are historical evidence and must be rerun when changes affect their scope. No images are viewed under the current user instruction.

Latest verified test release: **6a9df82f5e20059eddb45226** at https://jump-in-the-mix-test.netlify.app. Final local verification passed **382 unit/integration tests across 82 files, with no skips**, using one worker, and **142 browser cases with 36 intentional skips, zero failures, and zero retries** in the standard desktop/mobile Chromium projects. The published build passed **30 hosted states** across both themes and three widths. All 11 rendered CSS/JavaScript assets match the build; 13 contacts, five plans, and the plan fingerprint remain intact. See the [release qualification report](RELEASE_QUALIFICATION_2026-09-06.md) for historical failures, exact skip reasons, hosted diagnostics, and completed cleanup. This does not claim a complete GitHub Actions run or physical-device qualification. Earlier Firefox/WebKit and hosted native-zoom results remain historical evidence in the [implementation report](IMPLEMENTATION_REPORT_2026-09-05.md#native-browser-zoom-and-effective-ci-fixture-coverage).

| Requirement | Evidence needed | Current finding / next action |
| --- | --- | --- |
| Stripe payments before accepting real charges | Agreed pricing/access rules; reviewed billing integration; sandbox lifecycle and webhook tests; live account, products, credentials, and operational recovery configured | **Planned, not implemented.** The current free beta has no active billing. The owner has added Stripe to the roadmap; charging model, activation date, and treatment of existing beta accounts remain open. See the production launch plan for the proposed integration and tests. |
| First useful follow-up within minutes from a phone | Current account → business → person → prepared-message flow with measured elapsed time and relevant copy | Onboarding passes against the compiled production build in Chromium, Firefox and WebKit, including relevant copy, contact method, date and timezone. Preparation feedback is implemented and tested. Representative owner task timing remains open. |
| Immediately know who to contact next | Populated, empty, overdue, returning-from-composer, and delayed-preparation states across viewports | Today now reaches all matching follow-ups in batches of 30, prioritizes open work before completed history, and counts the complete filtered view. Cursor/filter changes protect drafts and restore heading focus. Preparation failure/recovery remains covered. Done, Skip and Undo now refresh full filtered totals and refill the batch while protecting drafts and Undo, including responses already in flight. Newer record revisions restore externally reopened work. |
| Responsive Today with a large due list | Signed-in readiness, first/repeated panel use and draft preservation with representative list sizes | Paging reduces the 300-record fixture from 7,091 to 893 initial DOM nodes. Median cold editor readiness changed from 5,504 to 2,536 ms; maximum-interaction medians changed from 360 to 152 ms cold and 448 to 112 ms warm in the measured slowed Chromium setup. Draft reopening remained intact. Shared-VM samples are variable; field Core Web Vitals remain unqualified. |
| Reliable unattended scheduling | Several observed timer-originated passes, due work completed with no user traffic, recovery after interruption | **Not qualified.** Previous Netlify scheduling checks and GitHub fallback gaps over 90 minutes do not establish reliable recurrence. The test site now uses 180 seconds for worker health: observed 200 at 177 seconds and 503 at 199 seconds. Activity-triggered wakeups work after applying WORKER_DISPATCH_MODE to all test-site contexts. A controlled handoff and a heartbeat after browsing are not proof of unattended scheduling. The optional [Cloudflare timer](CLOUDFLARE_SCHEDULER.md) remains inactive pending approval for the added service/secret and confirmation of Workers Free availability. |
| Saved edits promptly update prepared work | Committed changes wake processing; bounded backlog continuation; concurrency, retry, and error checks; published-runtime verification | Request-triggered wakeups retain durable leases and throttling. Preparation feedback, bounded polling, guarded retries, preserved notes, and deferred draft/Undo updates pass the local browser and database checks. Final hosted browsing created a new dispatch reservation and worker heartbeat after correcting the preview environment. Unattended timing remains a separate gate. |
| Flexible journey stages and automatic transitions | Milestone/timer/plan rules, skips/direct entry, overrides, pauses, timeline, concurrency, and tenant checks | Saved-rule editing, live destination previews, repeat-business returns, draft recovery and stale-edit protection are implemented. The current full unit/integration suite passes 382 tests with no skips. The final standard Chromium desktop/mobile browser suite passes 142 cases; the 30 hosted states include journey settings and the journey view. Earlier journey/calendar/intake checks passed twelve cases per Chromium, Firefox, and WebKit engine; those cross-engine results are historical. Hosted time-based behavior is limited by the unattended scheduling gate. |
| Broad calendar and lead intake compatibility | Standards fixtures, conflict/timezone handling, retries, matching/review, authorization, and real provider setup | iCalendar and generic intake fixtures pass. Native OAuth and real-time two-way editing are not implemented or claimed. Provider-specific account connections require actual setup and verification. |
| Clear and accessible application flows | DOM/keyboard/geometry and contrast across themes, sizes, expanded states, errors, and meaningful content | The current release passes 30 hosted states across both themes at 320/390/1440 CSS pixels, with one main landmark and zero axe violations, page errors, overflow, platform challenges, or account retries. Contact disclosures remain reachable beside the sticky Save bar; a deterministic regression passes in both Chromium projects. The final local run covers expanded sheets, selected-contact actions, completed/open paging, focus, errors, drafts, and real Chromium zoom through 400%. Earlier Firefox/WebKit checks remain historical. Physical devices and assistive technology remain unqualified. |
| Native phone handoffs and return recovery | Actual iPhone Safari and Android Chrome SMS/email/call/WhatsApp, keyboard and permission behavior | **Not run.** Keep the [manual device matrix](MANUAL_DEVICE_QUALIFICATION.md) authoritative; browser emulation does not qualify these rows. |
| Private browser state across accounts and outages | Real transport failure, legacy-worker upgrade, account changes in another tab, sign-out, stale responses and draft recovery | All 33 privacy/push cases pass across Chromium, Firefox and Linux WebKit. Push is session-bound and queued app notification text requires a current ownership/opt-in check. Same-account drafts and session-renewal opt-in are preserved. Physical-device history, background push verification and browser-generated notification behavior remain open. |
| Cross-browser and high-zoom behavior | Current Firefox/WebKit engine runs plus required physical Safari and desktop qualification | The final full run passes 142 cases in Chromium desktop/mobile. Local native Chromium zoom at 100/200/400% passes 12 pending/history/theme states at verified widths of 1280/640/320 CSS pixels, immediately after completed-history coverage. Earlier Firefox/WebKit history sweeps at six widths in both themes and the prior 12-state hosted native-zoom sweep remain historical; they were not repeated for this release. Current Firefox/WebKit reruns, Firefox native zoom, physical Safari, and device handoffs remain separate qualifications. |
| Honest, consent-respecting communication | Review default, optional auto-delivery, quiet hours, provider uncertainty and permanent receipts; real enabled-provider qualification | Automated controls are covered. The test site has no customer-delivery credentials; inbox/device/carrier delivery is not qualified. |
| Data ownership and recovery | Tenant isolation, exports/deletion/merge, encrypted backup and separate-database restore with current migrations | A fresh encrypted backup of the hosted test database restored into a separate loopback PostgreSQL database: all 70 tables, 24 current migrations and 95 declared foreign keys verified. Every table count and content digest matched, and application reads preserved 13 contacts, five plans and their prior fingerprint. The temporary restore database was removed. Recovery checks now discover new tables/relationships automatically and retain legacy archive compatibility. Durable off-host backup storage and provider-managed recovery remain separate operational qualifications. |
| Stable, performant hosted operation | Current static checks, build, browser tests, authenticated load, Lighthouse budgets, dependency audit, migration rehearsals, and container builds | Current local evidence is recorded in [the completion ledger](PRODUCT_COMPLETION.md). The authenticated load rehearsal now exercises six customer routes with populated accounts and per-route limits; see [Customer load qualification](CUSTOMER_LOAD_QUALIFICATION.md). Historical Netlify results establish only the behavior observed on that release. The intended hosted runtime, real CPU/memory limits, field performance, container execution and unattended scheduling still require qualification. |
| Product effectiveness and visual craft | Representative owner task sessions, follow-up usefulness, comprehension, and an authorized visual evaluation | No current owner-usability or visual-craft qualification supports an award claim. Preserve this gate rather than assigning an invented score. |

The earlier optional scheduler continuation passed 381 unit/integration tests (one skipped), 44 focused tests, static/type checks, the Wrangler dry run, and four local workerd cases. The current full suite result is recorded above. Production dependencies currently have zero reported vulnerabilities. These are local operations checks; they do not replace the hosted or physical-device gates above. The [implementation report](IMPLEMENTATION_REPORT_2026-09-05.md#optional-scheduler-prepared-and-qualified-locally) records the test environment, initial failures, and remaining activation requirements.

The latest completed test release is described in the [release qualification report](RELEASE_QUALIFICATION_2026-09-06.md); the [implementation report](IMPLEMENTATION_REPORT_2026-09-05.md) preserves earlier design and integration evidence. Private raw evidence stays in ignored `.artifacts/` directories, which also contain credentials and must not be published.


## Waitlist, referrals, and expanded administration — planned gates

The September 8 [infrastructure plan](ADMIN_OPERATIONS_INFRASTRUCTURE_PLAN.md) adds launch gates not covered by earlier release receipts. Before the first live wave, verify:

- Waitlist confirmation and withdrawal, duplicate submissions, and immediate removal from Waiting when a referral or wave grants access.
- Real PostgreSQL races between wave/referral issuance, account claims, capacity reservation, and the five-invite lifetime allowance.
- Per-recipient wave preview/release/pause/cancel, durable delivery, duplicate/out-of-order webhooks, and recovery from uncertain provider results.
- Direct-request authorization for every staff role/individual override, immediate revocation, MFA recovery, and last-Owner protection.
- Versioned System Mix and library publication/rollback, protected customer data, permission-gated exports, and platform audit records independent of customer workspaces.
- Reserved essential-email capacity, global admission controls, worker/backup/error alerts, a separate-database restore, and the intended Render runtime.

These gates remain incomplete as a whole. Waitlist waves and granular staff permissions now have local implementation and focused database/browser evidence; see [Waitlist operation](WAITLIST_OPERATIONS.md) and [Staff access](STAFF_ACCESS.md). This is not a production deployment receipt, and does not qualify the remaining operations, recovery, or device requirements.

September 8 staff/invitation checkpoint: 486 tests across 92 files, production build/static checks, all 28 migrations on populated and fresh schemas, and 20 focused Chromium cases on the standalone package pass locally. This covers the new waitlist, withdrawal, shared outbox, staff permissions and invitation operations, including mobile/light/dark accessibility. The final focused run has no reported server errors. Broader customer browser flows, real-device behavior, hosted scheduling, provider events, public policies, capacity limits and production qualification remain open in [the completion ledger](PRODUCT_COMPLETION.md).

September 8 email-controls checkpoint: signed provider events, deduplication/out-of-order reconciliation, suppression, atomic email budgets/account reserves, diagnostics, and opt-in defaults for new summaries are implemented locally. Current evidence: 500 tests/93 files, production build/typecheck/static checks, zero reported production dependency vulnerabilities, all 30 migrations in the populated/fresh and specialist rehearsals, and 21 focused standalone Chromium cases passed. The completion ledger records an initial transient 503 during concurrent local tooling and the subsequent successful checks. Real provider/inbox, load and production qualification, retention, suppression clearance and global admission ceilings remain open. See [Email operations](EMAIL_OPERATIONS.md).

September 8 account-administration checkpoint: customer suspension/restoration, session revocation, fresh permission/password/MFA checks, stale-revision protection, and active-account delivery guards are implemented locally. Current evidence: 510 tests across 94 files, nine focused account tests after the final concurrent-deletion guard, current production build/TypeScript/static checks, all 31 migrations in populated/fresh and specialist rehearsals, and 23 focused standalone Chromium cases passed. Production readiness requires administrator MFA. Staff onboarding, scoped support, reporting/revisions, public policies, capacity/retention, current broad customer/runtime qualification, and the newly requested voice PRD assessment/implementation remain in [the completion ledger](PRODUCT_COMPLETION.md).
