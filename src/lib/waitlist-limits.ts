// Shared with the public receipt so it can state the limit the server actually enforces
// instead of promising an email that a rate-limited request never sends.
export const WAITLIST_EMAIL_REQUESTS_PER_DAY = 3;
